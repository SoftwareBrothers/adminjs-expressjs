import { buildAuthenticatedRouter } from "./buildAuthenticatedRouter";
import { buildRouter } from "./buildRouter";

export { buildAuthenticatedRouter };
export { buildRouter };

/**
 * Plugin name
 * @static
 * @memberof module:@admin-bro/express
 */
export const name = "AdminBroExpressjs";
